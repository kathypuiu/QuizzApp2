<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admin.quizapp">
    
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LogInProf" />
        <activity android:name=".LogInStud" />
        <activity android:name=".Welcome" />
        <activity android:name=".SignInProf" />
        <activity android:name=".NewQuiz" />
        <activity android:name=".Intrebare_student" />
        <activity android:name=".StartNow_Profesor" />
        <activity android:name=".GenerareParola_Profesor" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".TopStudenti" />
        <activity android:name=".FinalCongrats_Student" />
        <activity android:name=".Questions_Prof"></activity>
    </application>

</manifest>